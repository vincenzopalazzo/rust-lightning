# Status revalidation — 2026-08-08

**Upstream HEAD checked:** `384e0d61305b24f5c6cdebe59c2c8f89b015d7b0` (`lightningdevkit/rust-lightning` main)

## Verdict: FIXED IN PRACTICE — do not refile

- Regression test present and **PASSING**:
  `ln::reorg_tests::test_reorg_resurrect_split_htlc_package_with_future_locktime`
- Path still has `assert!(request.merge_package(...).is_ok())` at
  `lightning/src/chain/onchaintx.rs:1184`, but only after
  `can_merge_with(...)`; non-mergeable packages go to `locktimed_packages`.
- Full campaign notes: `security-findings/fuzz/ldk-main-fuzz-2026-08-08.md`

```bash
cargo test -p lightning --features _test_utils --lib -- \
  ln::reorg_tests::test_reorg_resurrect_split_htlc_package_with_future_locktime
```
